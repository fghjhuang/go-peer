package bridgesdk

import (
	"fmt"
	"strings"
	"testing"
)

/*
* 测试信令服务器
 */
func TestSignal(t *testing.T) {
	//bridge :=Bridge{}
	//bridge.CreateSignalServer()
	output := []string{"1", "2"}
	fmt.Println(strings.Join(output, ",-----"))
}
